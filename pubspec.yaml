name: kasir_perdana
description: POS Perdana Cell & Parfum

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.0.0
  path: ^1.8.0
  provider: ^6.0.0

flutter:
  uses-material-design: true
